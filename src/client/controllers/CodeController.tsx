import { Controller } from "@flamework/core";

// code objects with expirations, etc
const VALID_CODES = [];

@Controller({})
export class CodeController {
  public claim(code: string): void {

  }

  public check(code: string): void {

  }
}
